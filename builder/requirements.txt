runpod~=1.7.9

faster-whisper==1.1.0

# PyTorch (CUDA 12.1 compatible wheels)
torch>=2.1.0
torchaudio>=2.1.0

# Speaker diarization — MUST stay <4.0: models.py uses use_auth_token,
# which pyannote 4.x removed (load fails silently -> diarization dead)
pyannote.audio>=3.1.0,<4.0
huggingface_hub>=0.23,<1.0

# Audio download
requests>=2.28.0